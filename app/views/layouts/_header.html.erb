<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to("MVP", root_path) %></h1>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#">Menu</a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <% if !current_user.nil? %>
      <li class="has-dropdown">
        <a href="#">Account</a>
        <ul class="dropdown">
          <li><%= link_to("Logout", destroy_user_session_path, method: :delete) %></li>          
        </ul>
        <% else %>
        <li><a href="#"><%= link_to("Sign Up", new_user_registration_path) %></a></li>      
        <li><a href="#" class="login-button">Login</a></li>  
        <% end %>
      </li>
    </ul>

  </section>
</nav>

<div class="row">
<div class="login large-40 columns large-centered">
<%= render "layouts/login" %>
</div>
</div>